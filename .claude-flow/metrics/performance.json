{
  "timestamp": "2026-07-15T03:29:29.580Z",
  "mode": "headless",
  "workerType": "optimize",
  "model": "sonnet",
  "durationMs": 97597,
  "executionId": "optimize_1784086071983_kh5yii",
  "success": true,
  "findings": {
    "sections": [
      {
        "title": "1. \"N+1\"-shaped: shared state causes every book card to redo work it doesn't need",
        "content": "\n`src/pages/Library.tsx:29-33` — every `BookCard` calls `useAllProgress()` indirectly via the shared store and recomputes `bookStats(book)` on **every render**, for **every book**, whenever **any single book's** progress changes anywhere in the app.\n\nThe root cause is in `src/lib/progress.ts:45-50`: `update()` spreads the *entire* `ProgressState` map into a new object on every mutation:\n```ts\nstore.set((s) => ({\n  ...s,\n  [bookId]: { ...sanitize(s[bookId]), ...patch, updatedAt: Date.now() },\n}))\n```\nSince `useAllProgress()` (`progress.ts:36-38`) returns that whole object via `useSyncExternalStore`, marking chapter 3 of *Book A* as read gives a new object reference to `useAllProgress()` subscribers — which re-renders **every** `BookCard` in the library, including books untouched by the change. Each of those re-renders redoes `bookStats(book)` (word counts + reduces over all chapters) for no reason.\n\n**Fix:** scope each card to its own book's progress and memoize the card:\n```tsx\n// Library.tsx\nconst BookCard = React.memo(function BookCard({ book, step }: { book: Book; step?: number }) {\n  const progress = useBookProgress(book.id)   // subscribes only to this book's slice\n  const stats = useMemo(() => bookStats(book), [book])\n  const pct = completionPct(book.map.chapters.length, progress)\n  ...\n})\n```\n`useBookProgress` already exists (`progress.ts:40-43`) and does the same `sanitize(all[bookId])` — it's just not used here. Swapping it in still re-renders on unrelated updates today because `useSyncExternalStore` returns the same `all` object reference check, but combined with `React.memo` and passing only `book`/`progress` as props, unrelated cards stop re-rendering once you select from the store with a proper equality check, or simpler: derive `progress` in the parent once via `useAllProgress()` and pass `all[book.id]` down as a prop with `React.memo`'s default shallow-prop comparison (won't help since `all` is a new ref) — the cleanest fix is a per-key selector store. Minimal version: memoize `bookStats` (below) so the redundant call is at least cheap, and wrap `BookCard` in `React.memo`.\n\n",
        "level": 2
      },
      {
        "title": "2. Unnecessary re-renders",
        "content": "\n**`AudioPlayer.tsx:69`** — `voicesForUi()` (filter + `.sort()` with string ops in `voiceScore`) runs inline in JSX on every render of `AudioPlayer`. `AudioPlayer` re-renders on every `useTts()` state change, which fires on every TTS sentence-boundary event (multiple times per second during playback) since `emit()` (`tts.ts:47-50`) always creates a new state object and `useSyncExternalStore` re-renders all subscribers. That's a sort + string manipulation over the whole voice list running many times a second for no reason — the voice list only actually changes when `voicesVersion` bumps.\n\n```tsx\n// AudioPlayer.tsx\nconst voiceList = useMemo(() => voicesForUi(), [s.voicesVersion])\n...\n{voiceList.map((v) => ...)}\n```\n\n**`Reader.tsx:100-109`** — the scroll handler calls `setPct` on every raw `scroll` event with no throttling:\n```ts\nwindow.addEventListener('scroll', onScroll, { passive: true })\n```\nOn a long chapter this fires dozens of times per second, each one a state update and re-render of the whole `Reader` tree. Throttle via `requestAnimationFrame`:\n```ts\nlet ticking = false\nconst onScroll = () => {\n  if (ticking) return\n  ticking = true\n  requestAnimationFrame(() => {\n    const el = document.documentElement\n    const max = el.scrollHeight - window.innerHeight\n    setPct(max > 0 ? Math.min(100, (window.scrollY / max) * 100) : 100)\n    ticking = false\n  })\n}\n```\n\n**`BookCard` / `MapChapterCard`** (`Library.tsx:29`, `BookMap.tsx:22`) aren't wrapped in `React.memo`, so any parent re-render (search keystroke, progress change) re-renders every card even when its own props are unchanged.\n\n",
        "level": 2
      },
      {
        "title": "3. Caching opportunities",
        "content": "\n**`content.ts:110-145` `loadChapter`** re-parses the same markdown (regex splitting into key-ideas/in-practice/body, plus `mdToHtml`) every time it's called — including navigating back to a chapter you already visited in the same session. The underlying dynamic import is cached by the browser module system, but the parsing work is not. Add a simple in-memory cache since chapter content is static for the app's lifetime:\n```ts\nconst chapterCache = new Map<string, Chapter>()\n\nexport async function loadChapter(bookId: string, n: number): Promise<Chapter | null> {\n  const key = `${bookId}/${n}`\n  const cached = chapterCache.get(key)\n  if (cached) return cached\n  const loader = chapterIndex.get(bookId)?.get(n)\n  if (!loader) return null\n  const raw = await loader()\n  // ...existing parsing...\n  const chapter = { number: n, title, words, minutes, keyIdeas, bodyHtml: mdToHtml(bodyMd), inPractice }\n  chapterCache.set(key, chapter)\n  return chapter\n}\n```\n\n**`content.ts:154-164` `bookStats`** is a pure function of `Book`, and `Book` objects are static module-level data (loaded once via `import.meta.glob`). It's currently recomputed from scratch (word-counting every chapter summary) on every call — once per `BookCard` render, plus once per book in `Library.tsx:109-113`'s totals `useMemo`. Cache by book id:\n```ts\nconst statsCache = new Map<string, BookStats>()\nexport function bookStats(b: Book): BookStats {\n  const cached = statsCache.get(b.id)\n  if (cached) return cached\n  // ...existing computation...\n  const stats = { mapMinutes, deepMinutes, totalMinutes, distilledPages, originalMinutes, savedPct }\n  statsCache.set(b.id, stats)\n  return stats\n}\n```\n\n**`markdown.tsx:5-7` `mdToHtml`** — `MapChapterCard` (`BookMap.tsx:42`) calls `<Markdown text={chapter.summary}>` for every chapter, and that re-parses the summary through `marked.parse` on every `BookMap` re-render (e.g. every time you toggle \"Map was enough\" on any chapter, since that flips `progress` state and re-renders the whole map list). Chapter summaries are static strings — memoize by content:\n```tsx\nconst htmlCache = new Map<string, string>()\nexport function mdToHtml(src: string): string {\n  let html = htmlCache.get(src)\n  if (html === undefined) {\n    html = marked.parse(src, { async: false }) as string\n    htmlCache.set(src, html)\n  }\n  return html\n}\n```\n(Uncapped cache is fine here — the input set is the fixed corpus of book/chapter markdown, not user-generated text.)\n\n",
        "level": 2
      },
      {
        "title": "4. Memory leaks",
        "content": "\nNothing acute — cleanup is handled carefully throughout (`ttsStop()` on unmount in `BookMap.tsx:81`/`Reader.tsx:92`, listener/interval teardown in `tts.ts`'s `stopKeepalive`, highlight/class cleanup in `follow.ts:100-106`). Two things worth a second look, not urgent:\n\n- **`tts.ts:70-73`**: `speechSynthesis.addEventListener('voiceschanged', refreshVoices)` is registered once at module scope and never removed. Harmless since it's a true app-lifetime singleton, but if this module were ever hot-reloaded repeatedly in dev, listeners would accumulate. Not worth fixing for a page-lifetime singleton.\n- **`follow.ts:96-108` `rangeForNormalizedOffsets`** walks the entire element's DOM with a fresh `TreeWalker` on every sentence-boundary event rather than resuming from the last position. Not a leak, but on a very long chapter body this is redundant work fired multiple times per second during playback — could track `pos`/node across calls if it ever shows up as a bottleneck.\n\n",
        "level": 2
      },
      {
        "title": "5. Redundant computations",
        "content": "**`content.ts:173-192` `searchBooks`** — for every keystroke, iterates all books and for each one calls `categoryOf(b)` (`content.ts:69-71`, a linear `.find()` over categories) and rebuilds/lowercases three template strings, even though `strong`/`mid`/`weak` don't change between searches for the same book. Since `books` and `categories` are static, precompute the searchable, lowercased strings once at module load instead of on every call:\n```ts\nconst searchIndex = books.map((b) => ({\n  book: b,\n  strong: `${b.title} ${b.author}`.toLowerCase(),\n  mid: `${b.tagline} ${categoryOf(b)?.name ?? ''}`.toLowerCase(),\n  weak: b.map.chapters.map((c) => c.title).join(' ').toLowerCase(),\n}))\n\nexport function searchBooks(query: string): Book[] {\n  const q = query.trim().toLowerCase()\n  if (!q) return books\n  const terms = q.split(/\\s+/)\n  return searchIndex\n    .map(({ book, strong, mid, weak }) => {\n      let score = 0\n      for (const t of terms) {\n        if (strong.includes(t)) score += 5\n        if (mid.includes(t)) score += 2\n        if (weak.includes(t)) score += 1\n      }\n      return { book, score }\n    })\n    .filter((x) => x.score > 0)\n    .sort((x, y) => y.score - x.score)\n    .map((x) => x.book)\n}\n```\n\n**`AudioPlayer.tsx:16`** — `touchDevice` correctly is computed once at module load (good pattern), but `voiceScore` (`tts.ts:77-91`) does several `.includes()` calls per voice, re-run for the full voice list on every `AudioPlayer` render as noted in §2 — folding that into the `voicesVersion`-keyed memo fixes both the re-render and the redundant computation in one change.\n\n---\n\n**Priority if you want to tackle a subset:** #1 (progress store causing library-wide re-renders) and the `voicesForUi()` memo in #2 are the two with real user-visible impact — the first degrades with library size, the second runs continuously during every audio session. The caching items in #3 are cheap wins with no behavior change since all the underlying data is static.",
        "level": 2
      }
    ],
    "codeBlocks": [
      {
        "language": "ts",
        "code": "store.set((s) => ({\n  ...s,\n  [bookId]: { ...sanitize(s[bookId]), ...patch, updatedAt: Date.now() },\n}))"
      },
      {
        "language": "tsx",
        "code": "// Library.tsx\nconst BookCard = React.memo(function BookCard({ book, step }: { book: Book; step?: number }) {\n  const progress = useBookProgress(book.id)   // subscribes only to this book's slice\n  const stats = useMemo(() => bookStats(book), [book])\n  const pct = completionPct(book.map.chapters.length, progress)\n  ...\n})"
      },
      {
        "language": "tsx",
        "code": "// AudioPlayer.tsx\nconst voiceList = useMemo(() => voicesForUi(), [s.voicesVersion])\n...\n{voiceList.map((v) => ...)}"
      },
      {
        "language": "ts",
        "code": "window.addEventListener('scroll', onScroll, { passive: true })"
      },
      {
        "language": "ts",
        "code": "let ticking = false\nconst onScroll = () => {\n  if (ticking) return\n  ticking = true\n  requestAnimationFrame(() => {\n    const el = document.documentElement\n    const max = el.scrollHeight - window.innerHeight\n    setPct(max > 0 ? Math.min(100, (window.scrollY / max) * 100) : 100)\n    ticking = false\n  })\n}"
      },
      {
        "language": "ts",
        "code": "const chapterCache = new Map<string, Chapter>()\n\nexport async function loadChapter(bookId: string, n: number): Promise<Chapter | null> {\n  const key = `${bookId}/${n}`\n  const cached = chapterCache.get(key)\n  if (cached) return cached\n  const loader = chapterIndex.get(bookId)?.get(n)\n  if (!loader) return null\n  const raw = await loader()\n  // ...existing parsing...\n  const chapter = { number: n, title, words, minutes, keyIdeas, bodyHtml: mdToHtml(bodyMd), inPractice }\n  chapterCache.set(key, chapter)\n  return chapter\n}"
      },
      {
        "language": "ts",
        "code": "const statsCache = new Map<string, BookStats>()\nexport function bookStats(b: Book): BookStats {\n  const cached = statsCache.get(b.id)\n  if (cached) return cached\n  // ...existing computation...\n  const stats = { mapMinutes, deepMinutes, totalMinutes, distilledPages, originalMinutes, savedPct }\n  statsCache.set(b.id, stats)\n  return stats\n}"
      },
      {
        "language": "tsx",
        "code": "const htmlCache = new Map<string, string>()\nexport function mdToHtml(src: string): string {\n  let html = htmlCache.get(src)\n  if (html === undefined) {\n    html = marked.parse(src, { async: false }) as string\n    htmlCache.set(src, html)\n  }\n  return html\n}"
      },
      {
        "language": "ts",
        "code": "const searchIndex = books.map((b) => ({\n  book: b,\n  strong: `${b.title} ${b.author}`.toLowerCase(),\n  mid: `${b.tagline} ${categoryOf(b)?.name ?? ''}`.toLowerCase(),\n  weak: b.map.chapters.map((c) => c.title).join(' ').toLowerCase(),\n}))\n\nexport function searchBooks(query: string): Book[] {\n  const q = query.trim().toLowerCase()\n  if (!q) return books\n  const terms = q.split(/\\s+/)\n  return searchIndex\n    .map(({ book, strong, mid, weak }) => {\n      let score = 0\n      for (const t of terms) {\n        if (strong.includes(t)) score += 5\n        if (mid.includes(t)) score += 2\n        if (weak.includes(t)) score += 1\n      }\n      return { book, score }\n    })\n    .filter((x) => x.score > 0)\n    .sort((x, y) => y.score - x.score)\n    .map((x) => x.book)\n}"
      }
    ]
  },
  "rawOutputPreview": "This is a static-content SPA (no backend/DB), so there's no literal \"N+1 SQL\" — I've mapped that category to its closest real analog: redundant per-item recomputation triggered by shared state. Here's what I found, in priority order.\n\n## 1. \"N+1\"-shaped: shared state causes every book card to redo work it doesn't need\n\n`src/pages/Library.tsx:29-33` — every `BookCard` calls `useAllProgress()` indirectly via the shared store and recomputes `bookStats(book)` on **every render**, for **every book**, whenever **any single book's** progress changes anywhere in the app.\n\nThe root cause is in `src/lib/progress.ts:45-50`: `update()` spreads the *entire* `ProgressState` map into a new object on every mutation:\n```ts\nstore.set((s) => ({\n  ...s,\n  [bookId]: { ...sanitize(s[bookId]), ...patch, updatedAt: Date.now() },\n}))\n```\nSince `useAllProgress()` (`progress.ts:36-38`) returns that whole object via `useSyncExternalStore`, marking chapter 3 of *Book A* as read gives a new object reference to `useAllProgress()` subscribers — which re-renders **every** `BookCard` in the library, including books untouched by the change. Each of those re-renders redoes `bookStats(book)` (word counts + reduces over all chapters) for no reason.\n\n**Fix:** scope each card to its own book's progress and memoize the card:\n```tsx\n// Library.tsx\nconst BookCard = React.memo(function BookCard({ book, step }: { book: Book; step?: number }) {\n  const progress = useBookProgress(book.id)   // subscribes only to this book's slice\n  const stats = useMemo(() => bookStats(book), [book])\n  const pct = completionPct(book.map.chapters.length, progress)\n  ...\n})\n```\n`useBookProgress` already exists (`progress.ts:40-43`) and does the same `sanitize(all[bookId])` — it's just not used here. Swapping it in still re-renders on unrelated updates today because `useSyncExternalStore` returns the same `all` object reference check, but combined with `React.memo` and passing only `book`/`progress` as props, unrelated cards stop r",
  "rawOutputLength": 9582
}